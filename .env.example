# Mailchimp integration for the newsletter signup form (/api/subscribe).
# Without these set, signups are logged to the server console instead of
# being sent to Mailchimp — useful for local development.
MAILCHIMP_API_KEY=
MAILCHIMP_SERVER_PREFIX=
MAILCHIMP_AUDIENCE_ID=
